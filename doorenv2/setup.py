from setuptools import setup

setup(name='doorenv-b1',
      version='0.0.1',
      install_requires=['gym']
)

# setup(name='doorenv-b2',
#       version='0.0.1',
#       install_requires=['gym']
# )

setup(name='doorenv-bax',
      version='0.0.1',
      install_requires=['gym']
)